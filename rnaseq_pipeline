#!/usr/bin/env bash
# Launcher: ./rnaseq_pipeline [--dry-run] [--project DIR] [--help]
set -euo pipefail
HERE="$(cd "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")" && pwd)"
export PYTHONNOUSERSITE=1
exec python3 "$HERE/rnaseq_pipeline.py" "$@"
